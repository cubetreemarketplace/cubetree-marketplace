# config.yml

backend:
  name: github
  repo: cubetreemarketplace/cubetree-marketplace
  branch: cms

  media_folder: 'assets/uploads'

collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    fields:
      - { name: Title }